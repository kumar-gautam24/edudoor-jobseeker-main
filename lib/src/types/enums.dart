enum Environment { development, staging, testing, production }

// enum JobsStatus { initial, success, failure }

// enum CourseStatus { initial, success, failure }

enum DeviceType { mobile, tablet, web, mac, windows, linux, fuchsia }

enum SlideMode { left, top, bottom, right }

enum CustomLocationPermission { granted, denied, disable }

enum RequestDataType { json, formData }

enum EducationType { tenth, twelve, diploma, graduation, pg, doctorate, english, othersLanguage, technicalDegrees }

enum SelectBtn { addBtn }
